let regex = /harry/ ;

regex  = /^harr/ // starts with harr
regex = /ry$/ // ends with ry
regex = /h.rry/ // only one character at . pos 
regex = /h*rry/ // zero or more character at * pos
regex = /ha?rryi?/ // it check that a or i both are optional
regex = /h\*rry/ // it checks for h*rry

let = "harry is harry"

